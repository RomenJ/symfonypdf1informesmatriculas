{% extends 'base.html.twig' %}

{% block title %}Colegio{% endblock %}

{% block body %}
    <h1>Colegio</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ colegio.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ colegio.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ colegio.address }}</td>
            </tr>
            <tr>
                <th>Foundationdate</th>
                <td>{{ colegio.foundationdate ? colegio.foundationdate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_colegio_index') }}">back to list</a>

    <a href="{{ path('app_colegio_edit', {'id': colegio.id}) }}">edit</a>

    {{ include('colegio/_delete_form.html.twig') }}
{% endblock %}
